export const delay = 200;
const count = Infinity;
const items = [
    '#', '!', '@', '$', '%', '^', '&', '*', '(', ')', '-', '_', '+', '=',
  ];  

export const Constants = {
    delay,
    count,
    items
}

export default Constants;